import createMuiTheme from "@material-ui/core/styles/createMuiTheme";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1e87e5"
    },
    secondary: {
      main: "#fff" // green: #28a745
    }
  }
});

export default theme;
